#include "main.h"

/**
 * _islower - Checks for lowercase character
 * @c:Character to compare
 * Return: 1 if true 0 if false
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}

